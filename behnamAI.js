// behnamAI.js
while (playerOnField) {
  if (barg.alive()) {
    harass(barg);
    continue;
  }
  behnam.dashAttack();
}
if(stillAlive) {
 finishHim(powerAmount);
}
// insert the saltiest comment directed at Max below
